# Contact Local — Frontend (Prototype)

Scaffolded with Vite + React + TypeScript + Tailwind + minimal PWA files

Features included:
- Signup page that requests location permission and submits GeoJSON location
- Login page (email & password)
- Axios wrapper that attaches JWT from localStorage
- PWA manifest + service worker placeholder

Run locally:
- npm install
- npm run dev

Next steps:
- Add pages for Home (nearby), Groups, Chats, Status viewer
- Integrate Socket.io client with token-based auth
- Implement offline shell and caching strategies
- Add UI polish and mobile-first navigation
# socal_front
# local
